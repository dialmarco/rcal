%h1 Enter Your Practice
= form_tag(:controller => 'practice_entries', :action => 'create') do
  = select_tag "practice", options_from_collection_for_select(practices, "id", "name")
  %p= text_field_tag("amount")
  = submit_tag "Create Practice" 
